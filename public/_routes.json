{
  "version": 1,
  "description": "Exclude small static .mjs glue files under /ort/ from Pages Functions so they're served as static assets from dist/. The Function at /ort/[[path]].ts proxies R2 for the large .wasm bytes; .mjs glue stays in dist/ and is served directly.",
  "include": ["/*"],
  "exclude": ["/ort/*.mjs"]
}
