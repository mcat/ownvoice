{
  "version": 1,
  "description": "Exclude the small ORT .mjs glue files from Pages Functions so they're served as static assets from dist/. The Function at /ort/[[path]].ts proxies R2 for the large .wasm bytes; .mjs glue lives in dist/ at the same versioned path (e.g. dist/ort/v1.25.1/foo.mjs) and is served directly.",
  "include": ["/*"],
  "exclude": ["/ort/v*/*.mjs"]
}
